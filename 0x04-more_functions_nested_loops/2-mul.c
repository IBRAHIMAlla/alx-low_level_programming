#include "main.h"
/**
* mul - Checks for a digit 0 through 9
* @a: first number
* @b: second number
* Return: The result of multiplication between a and b
*/
int mul(int a, int b)
{
return (a * b);
}
